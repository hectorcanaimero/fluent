# Reparto del P0 del peine fino (2026-09-11)

Fuente: `docs/auditoria/2026-09-11-peine-fino.md`, sección 6, bloque P0. Objetivo: cerrar los 15 ítems P0 en **un solo PR** contra `main`.

## Estructura de ramas

| Rama | Sesión | Worktree (ya creado, sobre `origin/main` = `44f9193`) |
|---|---|---|
| `fix/p0-api` | **Opus** | `/root/projects/fluent/.claude/worktrees/p0-api` |
| `fix/p0-mobile` | **Sonnet** | `/root/projects/fluent/.claude/worktrees/p0-mobile` |
| `fix/p0-peine-fino` | planner | rama de integración: fusiona las dos anteriores y abre el único PR |

Reglas comunes:
- Trabajá **solo** en tu worktree. No toques `main` ni el otro worktree.
- Un commit por ítem, mensaje `fix(<ámbito>): MAL-nn <qué>` en español, con la atribución de tu sesión. Ámbitos: `api`, `mobile`.
- Antes de cada commit: móvil `flutter analyze && flutter test`; API `pnpm lint && pnpm test` (desde `apps/api`). Todo en verde, sin avisos nuevos.
- Antes de reportar, pasá `/code-review` sobre tu rama y corregí lo que salga.
- Al terminar: `git push -u origin <tu-rama>` y avisá a la sesión `planner [bc5a8d]` por SendMessage con la lista de commits y cualquier decisión que hayas tomado. **No abras PR.**
- Reglas del VPS: nada de `flutter run` ni servidores de desarrollo; `pnpm install --frozen-lockfile --prefer-offline` usa el store global; matá cualquier proceso en background que arranques.
- Copy de la app en **voseo** (es) y pt-BR informal; toda cadena nueva va a `app_es.arb` y `app_pt.arb` (el test de paridad lo exige).
- Si un ítem obliga a cambiar comportamiento descrito en una spec (`docs/specs`), actualizá el párrafo de la spec en el mismo commit; sin rediseños.
- Propiedad de archivos (para que la fusión sea limpia): no edites archivos del otro reparto. Si te hace falta, avisá a planner en vez de tocarlo.

## Sesión Opus · `fix/p0-api` (7 ítems, los de más criterio)

Archivos propios: todo `apps/api/**`; en móvil solo `apps/mobile/lib/core/http/**`, `apps/mobile/lib/features/auth/**`, `apps/mobile/lib/app/router.dart`, `apps/mobile/lib/app/splash_screen.dart`, `apps/mobile/lib/features/session/presentation/session_summary_screen.dart`, y sus tests. En los ARB, **agregá claves al final del archivo** con prefijo `auth…`.

Orden sugerido: MAL-17 → MAL-21 → MAL-19 → MAL-18 → MAL-02 → MAL-03 → MAL-04.

1. **MAL-17 · Bearer en los logs.** `apps/api/src/app.module.ts:33-40` y `worker.module.ts` configuran `pinoHttp` solo con `level`. Añadir `redact: ['req.headers.authorization', 'req.headers.cookie']` y `autoLogging: { ignore: (req) => req.url === '/v1/health' }`. Sacá la config a una función `buildPinoHttpOptions(env)` en `src/config/logger.ts` con test unitario que compruebe `redact` y el ignore de health, y usala en los dos módulos.
2. **MAL-21 · Deploy corta turnos.** En `src/main.ts`: `app.enableShutdownHooks()` y, tras `listen`, `app.getHttpServer().keepAliveTimeout = 65_000; headersTimeout = 66_000`. Mismo tratamiento para el worker si arranca un servidor HTTP (verificá `src/worker.ts`). Test: unitario sobre una función `configureHttpServer(server)`.
3. **MAL-19 · Bono de desafío auto-otorgable.** `src/sessions/sessions.service.ts:132` persiste `dto.challengeFromUserId` sin validar. En `openSession`, si viene el campo, pedir `ChallengesService.listChallenges(userId)` y exigir que exista un candidato con ese `userId` y el mismo `topic` (y `kind` si aplica); si no, `ApiError` 422 `CHALLENGE_NOT_AVAILABLE` (añadir al catálogo de códigos y a `i18n/es.json` y `pt-BR.json`). Tests: unitario en `sessions.service.spec.ts` (válido, inválido, ausente) y e2e en `test/sessions.e2e-spec.ts`. Nota en SPEC-07 (sección de desafíos): "el servidor valida el desafío al abrir la sesión".
4. **MAL-18 · CSRF en el callback de OpenRouter + redirección abierta.** Hoy `GET /v1/providers/openrouter/callback/:id` (`@Public`) canjea el `code` y escribe la credencial del usuario que inició el flujo. Cambiar a:
   - El callback público **solo** guarda el `code` en Redis bajo la entrada PKCE (`pkce.store.ts`, TTL 10 min, misma clave que el `code_verifier`) y redirige a `fluent://oauth/openrouter?done=1` (o `?error=…`). No canjea nada.
   - `POST /v1/providers/openrouter/pkce/complete` (autenticado) recibe `{ "codeVerifierId": "<id>" }`; comprueba que la entrada existe y que `entry.userId === user.id` (si no, 403 uniforme), lee el `code` guardado, hace el intercambio con el `code_verifier` y persiste la credencial. Mantener compatibilidad: si el body trae `code` explícito (flujo antiguo) se ignora en favor del guardado. **Este contrato ya lo está implementando Sonnet en el móvil: no lo cambies.**
   - `isValidCallbackUrl` en `providers/pkce.ts:50-64`: solo aceptar el esquema/URL configurado (`OPENROUTER_OAUTH_CALLBACK` de `env.ts`) o `fluent://`; nada más.
   - Tests: unitarios de `providers.service` (callback guarda y no canjea; complete con owner correcto, con owner ajeno, sin code guardado, con entrada expirada) y e2e de `test/providers.e2e-spec.ts`. Actualizar SPEC-02 §(PKCE) con el flujo nuevo en 4 líneas.
5. **MAL-02 · Sesión zombi al fallar el refresh.** `apps/mobile/lib/core/http/api_client.dart:59` borra tokens y nadie se entera. Añadir a `ApiClient` un callback `onSessionExpired` (o un `StreamController<void>`); `AuthController` se suscribe en su constructor y pasa a `AuthStatus.unauthenticated`. En `AuthController.logout()` (`auth_controller.dart:57`) llamar a `InsforgeAuthClient.logout(accessToken)` **antes** de borrar tokens, tolerando fallos de red (ya está preparado para eso, línea 96). Borrar cuenta pasa por `logout()`, así que queda cubierto. Tests: `test/features/auth/auth_controller_test.dart` (nuevo): refresh fallido → `unauthenticated`; logout llama al remoto.
6. **MAL-03 · Sin red = cerrar sesión.** `auth_controller.dart:325-328` (`_loadMe`) borra tokens ante cualquier error. Solo borrar si `ApiException.code == unauthenticated` (401). En cualquier otro error: conservar tokens, `AuthStatus.error` (nuevo estado en `auth_state.dart`) y que el router mande a `/` con `SplashScreen` mostrando "No pudimos conectar" + botón **Reintentar** que llame `bootstrap()` de nuevo (claves ARB `authOfflineTitle`, `authOfflineBody`, `authRetry`). Actualizar `computeRedirect` y `test/app/router_redirect_test.dart`.
7. **MAL-04 · Bucle al cerrar una sesión restaurada.** `router.dart:59-61` fuerza `/session/:id` mientras `activeSessionId` no sea null y nadie lo limpia al terminar. Añadir `AuthController.clearActiveSession(String id)` y llamarlo en `SessionSummaryScreen.initState` (la pantalla de resumen solo se llega con la sesión cerrada). Además `computeRedirect` no debe redirigir si la ruta actual ya es `/session/:id/summary`. Tests: router y summary.

## Sesión Sonnet · `fix/p0-mobile` (8 ítems + la mitad móvil de MAL-18)

Archivos propios: `apps/mobile/**` salvo los reservados a Opus arriba. Podés editar `pubspec.yaml`, `android/**`, `ios/**`, `README.md` de mobile y `docs/runbooks/probar-app-movil.md`. En los ARB, insertá claves nuevas junto a las de su pantalla (Opus agrega al final; si hay conflicto lo resuelve planner).

Orden sugerido: MAL-01 → MAL-15 → MAL-16 → MAL-06 → MAL-09 → MAL-11 → MAL-13 → MAL-12 → MAL-18 (móvil).

1. **MAL-01 · iOS se cierra al pedir micrófono.** En `ios/Runner/Info.plist` añadir `NSMicrophoneUsageDescription` y `NSSpeechRecognitionUsageDescription` con el texto (en español, es la única locale del plist hoy): "Fluent usa el micrófono para que hables con el tutor. La transcripción se procesa en tu dispositivo." Sin test posible; verificá que el plist sigue siendo XML válido (`plutil` no existe aquí: `python3 -c "import plistlib,sys; plistlib.load(open(sys.argv[1],'rb'))" ios/Runner/Info.plist`).
2. **MAL-15 · Fuente no empaquetada.** `lib/app/theme.dart:42` declara `PlusJakartaSans` y `pubspec.yaml` no tiene `fonts:`. Descargá la variable font oficial `https://github.com/google/fonts/raw/main/ofl/plusjakartasans/PlusJakartaSans%5Bwght%5D.ttf` (y el `OFL.txt` al lado) a `assets/fonts/`, declarala en `pubspec.yaml` bajo `fonts:` con `family: PlusJakartaSans` (Flutter mapea `FontWeight` al eje `wght`). Si `flutter test` muestra avisos de fuente, generá instancias estáticas 500/600/700/800 (`pip install fonttools` + `fonttools varLib.instancer`). Añadí un test en `test/app/` que compruebe que `ThemeData.textTheme.bodyMedium!.fontFamily == 'PlusJakartaSans'`.
3. **MAL-16 · Identidad de plantilla.** (a) `android:label="Fluent"` en `AndroidManifest.xml:8`, `CFBundleDisplayName` y `CFBundleName` = `Fluent` en `Info.plist`. (b) Icono: generá `assets/icon/icon.png` (1024×1024) con Python PIL (está instalado): fondo `#0E9C8C` con esquinas redondeadas, una burbuja de diálogo blanca centrada y dentro una llama `#F0813B` simple (dos elipses); sin texto. Añadí `flutter_launcher_icons` como dev dependency con `android: true`, `ios: true`, `adaptive_icon_background: "#0E9C8C"`, `adaptive_icon_foreground` = la burbuja sobre transparente (`assets/icon/foreground.png`), y ejecutá `dart run flutter_launcher_icons`. Comprobá con `md5sum` que los mipmap ya no coinciden con `/opt/flutter/packages/flutter_tools/templates/app_shared/android.tmpl/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png.img.tmpl`. (c) Firma: en `android/app/build.gradle.kts` cargar `android/key.properties` si existe (`storeFile`, `storePassword`, `keyAlias`, `keyPassword`) en un `signingConfigs.create("release")` y usarlo en `buildTypes.release`; si el archivo no existe, caer a `debug` con un `logger.warn`. Añadí `android/key.properties` y `*.jks` a `.gitignore`. **No crees ningún keystore**; documentá en `README.md` de mobile cómo generarlo en la Mac. (d) `android/app/proguard-rules.pro` con las reglas keep de `flutter_local_notifications` (`-keep class com.dexterous.** { *; }` y las de GSON que pide su README) y referenciarlo en `buildTypes.release` con `proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")`.
4. **MAL-06 · TTS al doble de velocidad.** `lib/features/session/data/tts_service.dart:160-163` pasa 0.8/1.0/1.2 crudos. Mapear `native = 0.5 * uiRate` (0.4/0.5/0.6) dentro del servicio, documentarlo en el doc comment, y en `conversation_screen.dart` aplicar `setSpeechRate(_ttsRate)` antes de **cada** `speak` (línea ~323), no solo en repetir. Test en `test/features/session/` con el fake/mocktail del plugin comprobando el valor nativo.
5. **MAL-09 · Spinner infinito.** Crear `lib/core/widgets/async_body.dart`: `AsyncBody<T>({required AsyncSnapshot<T> snapshot, required Widget Function(T) builder, required VoidCallback onRetry})` que muestra loading, o error con mensaje (`commonLoadErrorTitle`, `commonLoadErrorBody`) y botón `commonRetry`, o el contenido. Usarlo en `new_session_screen.dart:111`, `progress_screen.dart:35`, `group_screen.dart:84`, `memory_screen.dart:151`, `settings_screen.dart:95` (el `onRetry` reasigna el future y hace `setState`). En `conversation_screen.dart:81-104` envolver `_bootstrap` en `try/catch`: en error, `_loading = false` y un estado `_bootError` que pinte el mismo `AsyncBody`-like con **Reintentar** (vuelve a `_bootstrap`) y **Volver** (`context.go('/')`). Tests: un widget test de `AsyncBody` y uno por pantalla con `FakeApi` que lance.
6. **MAL-11 · Callejón sin salida en Proveedores.** En `providers_screen.dart`, cuando hay al menos un proveedor activo mostrar un botón primario fijo abajo "Ir a practicar" (`providersGoPractice`) que hace `context.go('/')`; y si `context.canPop()` mostrar la flecha atrás en el AppBar. Test de widget: con proveedor activo aparece el botón y navega.
7. **MAL-13 · Bloqueo por proveedor inconsistente.** Un único `bool canPractice` derivado en `home_data.dart` (hay proveedor activo). Usarlo en el CTA de Home (`home_screen.dart:349-366`), en los chips de temas rápidos (`:496`) y en la pestaña Practicar de `home_shell.dart:34-36`: si no se puede, `context.go('/providers')` y un `SnackBar` con `homeNeedProviderHint` en vez de crear la sesión. Tests en `home_screen_test.dart`.
8. **MAL-12 · Timezone fija.** `onboarding_flow.dart:85` manda `America/Argentina/Buenos_Aires`. Añadir `flutter_timezone` a `pubspec.yaml` y usar `FlutterTimezone.getLocalTimezone()` con fallback `'UTC'` si lanza; en tests, inyectar por provider (`timezoneProvider`) para no depender del plugin. Quitar el TODO.
9. **MAL-18 (móvil) · Deep link sin `state`.** En `providers_screen.dart:79-88` eliminar la rama que acepta `code` por deep link. Nuevo contrato (Opus lo implementa en la API, no lo cambies): tras `done=1`, el móvil llama `POST /providers/openrouter/pkce/complete` con body `{ "codeVerifierId": pkce.codeVerifierId }` (sin `code`) y luego refresca el estado del proveedor; si el deep link trae `error=…`, mostrar `providersOauthError`. Actualizar `fluent_api.dart` (firma `completeOpenRouterPkce({required String codeVerifierId})`), `http_fluent_api.dart:91`, `fake_api.dart` y los tests de providers.

## Integración (planner)

1. Esperar los dos avisos. Fusionar `fix/p0-api` y `fix/p0-mobile` en `fix/p0-peine-fino`; resolver conflictos (esperables solo en ARB y `pubspec.lock`).
2. Correr las cuatro verificaciones sobre la rama integrada.
3. Abrir el PR `fix(p0): peine fino, bloqueantes y seguridad` con la lista de los 15 ítems y avisar al owner, que mergea y despliega en Coolify.
4. Marcar los ítems en el checklist del artefacto.
