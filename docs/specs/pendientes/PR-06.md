# Pendientes y decisiones sin spec — PR-06

Entradas registradas por la sesión de PR-06 (app móvil Flutter). Formato: `PEND-nn · spec afectada
· qué falta o qué discrepancia se encontró · decisión`.

**Nota de recuperación (2026-09-09, T9):** hasta esta entrada, este archivo tenía por error el
contenido completo de `docs/specs/pendientes/PR-08.md` (verificado con `diff`: idéntico salvo el
título) desde el commit que lo creó (`b48a735`, "pendientes de PR-06 en archivo propio") — un
copy/paste equivocado al bifurcar el archivo, nunca corregido por la recuperación de fusión
posterior (`bd938a8`). No había ninguna entrada real de PR-06 (app móvil) en este archivo. Ese
contenido de PR-08 sigue intacto en `docs/specs/pendientes/PR-08.md`, así que no se perdió nada al
reemplazarlo aquí por las entradas reales de esta tarea.

## PEND-01 · SPEC-02 §4.1 · `providers[].status` de `GET /me`: la app comparaba contra `'connected'`, la API real devuelve `'active'`

`apps/api/src/credentials/credentials.repository.ts` (`ProviderConnectionStatus`) usa los cuatro
valores `'active' | 'revoked' | 'error' | 'not_connected'` (`CredentialStatus` de SPEC-01 §2.4 más
el sintético `'not_connected'` de PEND-15/PR-02). La app comparaba en ocho sitios contra el literal
`'connected'`, que la API real nunca envía — con `USE_FAKE_API=false` el banner "Conectá un
proveedor para practicar" habría bloqueado la práctica siempre, aunque el usuario tuviera un
proveedor activo, y la pantalla de proveedores nunca habría mostrado "Conectado" ni créditos.

**Corrección:** todos los `== 'connected'` pasan a `== 'active'`:
`lib/features/home/domain/home_data.dart::hasActiveProvider`,
`lib/features/providers/domain/providers_data.dart::isConnected`,
`lib/features/providers/presentation/providers_screen.dart` (carga de status, `isConnected` local,
switch de texto de estado), `lib/features/onboarding/presentation/onboarding_flow.dart` (salto a
`/providers` tras el onboarding), y los nueve usos de `'connected'` en `lib/core/api/fake_api.dart`
(para que la simulación deje de divergir del contrato real). `'revoked'` cae al mismo texto que
`'not_connected'` (rama `_` del switch): la UI no distingue "nunca conectado" de "revocado", que es
lo que ya hacía antes de este fix para cualquier valor que no fuera `'connected'`/`'error'`.

## PEND-02 · SPEC-02 §4.1 · `GET /me` no exponía `pendingActions`

`apps/api/src/profiles/profiles.types.ts::MeDto.pendingActions` (`string[]`, ver
`docs/specs/pendientes/PR-02.md` PEND-76) no existía en `MeResponse` (`models.dart`). Se agrega
`@Default(<String>[]) List<String> pendingActions` (freezed/json_serializable regenerados). Como
solo `WEEKLY_SUMMARY_NEEDS_CREDENTIAL` existe hoy y la API la calcula ya filtrada al owner del
grupo (PEND-76 de PR-02: la clave de Redis es `<ownerId>` = el `userId` de quien pide `/me`), la
app no necesita comprobar el rol: si la lista no está vacía, la acción es para quien la pide.
`HomeData.hasWeeklySummaryCredentialPending` expone ese único caso conocido; cualquier otro
identificador futuro se ignora sin romper nada (mismo criterio que ya documentó la API).

**Banner en Home:** `_PendingActionBanner` (`home_screen.dart`), visible cuando
`hasWeeklySummaryCredentialPending` es verdadero, con botón que navega a `/providers`. No hay
pantalla ni copy de referencia en SPEC-06 para este caso (la spec es de antes de PR-05); se optó
por el patrón ya usado para `_NoProviderBanner` (mismo estilo `goldSoft`) en vez de inventar un
componente nuevo.

## PEND-03 · SPEC-02 §6 · `ApiErrorCode` (Dart) no cubría `NOT_FOUND` ni `INTERNAL`

`apps/api/src/common/api-error.ts::API_ERROR_STATUS` tiene 16 códigos (los 14 de la tabla de
SPEC-02 §6 más las dos extensiones documentadas ahí mismo, `NOT_FOUND` 404 e `INTERNAL` 500,
añadidas por PR-02/T3). `ApiErrorCode` (`lib/core/errors/api_exception.dart`) solo tenía 14 más el
valor sintético `unknown`; un 404 de ruta inexistente o un 500 no controlado caían en `unknown`
igual que un error de red o un código realmente desconocido, indistinguibles para quien capture la
excepción. Se agregan `notFound` e `internal` a `ApiErrorCode` y a `fromWire`.

## PEND-04 · SPEC-02 §6 · `SESSION_ALREADY_ACTIVE`: `activeSessionId` va en el cuerpo, no dentro de `details`

`ApiException` (`apps/api/src/common/api-error.ts`) agrega `activeSessionId` como campo **extra**
junto a `error`/`message`/`statusCode` (`extra` se mezcla en el cuerpo top-level, nunca dentro de
`details[]`, que es solo para `VALIDATION`). El `ApiException` de Dart no tenía forma de leer ese
campo: `ApiClient.mapDioException` solo parseaba `data['details']`. Se agrega
`ApiException.activeSessionId` (`String?`) y `mapDioException` lo lee de `data['activeSessionId']`.
`FakeApi.createSession` metía el `activeSessionId` dentro de `details` (`[{'activeSessionId': ...}]`)
para simular el error: se corrige para usar el campo nuevo, consistente con la forma real. Ningún
test dependía de la forma vieja (no hay assertions sobre `details` de `SESSION_ALREADY_ACTIVE`).
Sesiones (`/sessions*`) no está fusionado (PR-04): esta corrección se valida contra el contrato de
`api-error.ts` (ya en `main`) y SPEC-02 §6, no contra un controlador de sesiones real.

## PEND-05 · SPEC-02 §4.5 · `getWeeklySummary` nunca detectaba `NOT_READY`

`HttpFluentApi.getWeeklySummary` distinguía "no hay resumen todavía" (`404 NOT_READY`) de un error
real con `e.toString().contains('NOT_READY')`. Como `ApiException.toString()` imprime el nombre
Dart del enum (`ApiErrorCode.notReady`), nunca la cadena `NOT_READY` de la API, esa condición jamás
era verdadera: cualquier `404 NOT_READY` real habría vuelto a subir como excepción no capturada en
vez de convertirse en `null` (que es lo que espera `GroupScreen._load`, aunque ahí el `catch (_)` lo
disimulaba igual). Se reescribe para capturar el `ApiException` que ya arma `ApiClient.guard` y
comparar `e.code == ApiErrorCode.notReady`.

## PEND-06 · SPEC-02 §4.1, `apps/api/src/content/interests.json` · el catálogo de intereses local no coincidía con el real

`GET /me` devuelve `interestsCatalog` con los 24 ids de `apps/api/src/content/interests.json`
(`technology`, `videogames`, `movies-series`, …), fijados por PR-02/T2. `kFallbackInterests`
(`lib/features/onboarding/data/interests_catalog.dart`) y el switch de
`lib/features/onboarding/domain/interest_labels.dart` tenían 24 ids **distintos**, inventados antes
de que existiera el catálogo real (`tech`, `gaming`, `daily_life`, `news`, `pets`, `politics`… — la
mayoría sin equivalente exacto en el catálogo real). Como `OnboardingFlow._loadCatalog` ya prefiere
`me.interestsCatalog` sobre el fallback (`me.interestsCatalog.isNotEmpty ? ... : kFallbackInterests`,
sin tocar), contra la API real esto no rompía el flujo, pero sí la traducción: cualquier id real sin
entrada en el switch cae a `_ => id` (el id crudo en inglés/kebab-case, por ejemplo "movies-series"
sin traducir) en vez de una etiqueta localizada — justo lo que dice de evitar SPEC-06 §4.5 en
general para textos de la UI.

**Corrección:** `kFallbackInterests` pasa a ser una copia exacta (mismos 24 ids, mismo orden) de
`INTERESTS_CATALOG_IDS` (`apps/api/src/content/interests.json`); `interest_labels.dart` traduce los
24 ids reales; se agregan las 24 claves nuevas (`interestTechnology`, `interestVideogames`,
`interestMoviesSeries`, …) a `app_es.arb` y `app_pt.arb` **en este mismo commit**, con las
traducciones `label_es`/`label_pt` que ya trae `interests.json` (la API las tenía preparadas para
esto). `FakeApi` reutiliza `kFallbackInterests` en vez de duplicar la lista, y su perfil de ejemplo
usa ids reales (`'travel', 'technology', 'movies-series', 'music'`). Se actualizó
`test/features/onboarding/onboarding_flow_test.dart` (claves `onboarding_interest_tech` →
`onboarding_interest_technology`, `onboarding_interest_movies` →
`onboarding_interest_movies-series`).

## PEND-07 · SPEC-02 §4.3 · endpoints de `/sessions*` validados contra la spec, no contra código real

PR-04 (sesiones) no está fusionado en `main` todavía (`apps/api/src` no tiene módulo `sessions`).
Las rutas, cuerpos y respuestas de `HttpFluentApi` para `/sessions/suggestions`, `POST /sessions`,
`POST /sessions/:id/turns`, `POST /sessions/:id/end`, `GET /sessions`, `GET /sessions/:id` se
revisaron contra SPEC-02 §4.3 y SPEC-04 (no contra un controlador real) y coinciden campo por
campo con lo que describen esas dos specs. Cuando PR-04 se fusione, hay que repetir esta
comparación contra el código real (`apps/api/src/sessions/`, si se llama así) antes de dar por
buena esta entrada.

## PEND-08 · resto de endpoints revisados sin discrepancias

Comparados campo por campo contra los DTOs y mappers reales de `apps/api/src` (todos con
comentarios explícitos "no se toca `models.dart`", es decir, la API ya se construyó a propósito
para calzar con el modelo Dart existente): `GET /group`, `PUT /me/profile`,
`POST /invitations/redeem`, `POST /admin/invitations` (`profiles.types.ts`, `groups.controller.ts`);
`POST /providers/openrouter/pkce/{start,complete}`, `POST /providers/gemini`,
`DELETE /providers/:provider`, `GET /providers/:provider/status` (`providers.types.ts`,
`ProviderCredits` de `provider-api.client.ts`); `GET /models`, `PUT /me/models`
(`models/models.types.ts` — confirma PEND-48 de `docs/specs/pendientes/PR-02.md` ya resuelto:
`pricePerMillionUsd` único, precio de salida); `GET /memory`, `PATCH /memory/facts/:id`,
`DELETE /memory/facts/:id`, `PUT /memory/brief`, `DELETE /memory` (`memory.types.ts`,
`memory.mapper.ts`); `GET /progress` (`progress.types.ts`); `GET /leaderboard`, `GET /challenges`,
`GET /weekly-summary` (`social.types.ts`). Ninguno necesitó cambios en Dart más allá de los de
PEND-01 a PEND-06.

## PEND-09 · SPEC-04 §3.2 · la app mandaba `kind: 'topic'`, la API real espera `'free_topic'`

Con PR-04 (`feat/sesion`, PR #9) ya fusionado en `main`, `apps/api/src/sessions/dto/create-session.dto.ts`
(`SESSION_KINDS`) fija los cuatro valores válidos de `kind` en `POST /sessions`:
`'free_topic' | 'roleplay' | 'news' | 'boss'`. Tres sitios de `apps/mobile` mandaban el literal
`'topic'` (que no está en esa lista): `new_session_screen.dart` (tema de la lista y "Surprise me"),
`home_screen.dart` (temas rápidos de Home **y** el botón de Boss battle, que además mandaba
`kind: 'roleplay'` sin `roleplayId` en vez de `kind: 'boss'`) y `FakeApi.getChallenges` (dato de
ejemplo). Contra la API real, cualquiera de esos tres botones habría respondido siempre
`400 VALIDATION` (`resolveFreeTopic`/`resolveRoleplay` de `sessions.service.ts` no reconocen
`'topic'`, y `'roleplay'` sin `roleplayId` falla igual) — con `USE_FAKE_API=false` la práctica desde
Home y el "Boss battle" estaban rotos.

**Corrección:** los cuatro literales pasan a `'free_topic'` (`new_session_screen.dart`,
`home_screen.dart`) y `home_screen.dart::_startSession` ahora recibe `kind` explícito en vez de
inferirlo de si hay `topic` o no (`onBoss` manda `kind: 'boss'`, `_QuickTopics` manda
`kind: 'free_topic'`). De paso, `new_session_screen.dart`'s "Surprise me" tenía un bug
independiente: mandaba `kind: 'topic'` **sin** `topic`, pero `free_topic` exige un `topic` no vacío
de 2 a 80 caracteres (SPEC-04 §3.2); ese botón habría fallado con `400 VALIDATION` siempre, con o
sin el fix del literal. Se corrige eligiendo un tema al azar de `suggestions.topics` (las mismas 6
sugerencias de `GET /sessions/suggestions` que ya pinta la pestaña) en vez de mandar la petición sin
tema; si `topics` viene vacío el botón queda deshabilitado en lugar de fallar en el servidor.

## PEND-10 · SPEC-04 §4 y SPEC-03 §6 · `TurnResult` (Dart) no tenía `unavailable`

`apps/api/src/sessions/sessions.types.ts::TurnResultDto` agrega `unavailable?: boolean` a los
cuatro campos que ya tenía la app (`turnIdx`, `reply`, `corrections`, `modelUsed`, `degraded`): solo
está presente (y en `true`) cuando la cadena de modelos se agotó (SPEC-03 §6), ver
`turns.service.ts::persistTutorTurn`. El modelo Dart (`models.dart::TurnResult`) no lo tenía, así
que ese campo se perdía al parsear la respuesta. Se agrega `@Default(false) bool unavailable`
(freezed/json_serializable regenerados): el `false` por defecto cubre tanto la ausencia del campo
(turno normal) como cualquier `false` explícito.

## PEND-11 · SPEC-04 §4 · `POST /sessions/:id/turns` nunca lanza `503 LLM_UNAVAILABLE`

`conversation_screen.dart::_send()` capturaba `ApiException` con
`e.code == ApiErrorCode.llmUnavailable` para contar tres intentos seguidos y ofrecer terminar la
sesión — un comportamiento copiado de la tabla de errores de SPEC-02 §6, que sí lista
`LLM_UNAVAILABLE: 503`. Pero `apps/api/src/sessions/turns.service.ts` (T2 de PR-04, ya fusionado)
atrapa `LlmUnavailableError` **dentro** de `addTurn` y responde igual `200` con
`TurnResult(degraded: true, unavailable: true, reply: DEGRADED_REPLY, modelUsed: null)`: el turno
del usuario se persiste igual (avanza `turns_count`) y la conversación sigue. `/turns` nunca deja
escapar un `503` (ver también `sessions.service.ts` para la apertura, mismo patrón). Contra la API
real, el `catch (ApiException)` de la pantalla jamás se disparaba por esta causa: los tres intentos
seguidos con la cadena agotada habrían mostrado tres burbujas del tutor con el texto de disculpa fijo
y el chip "degradado", pero nunca el diálogo de "no podemos conectar con el tutor".

**Corrección:** el conteo de intentos fallidos ahora mira `result.unavailable` en la rama de éxito,
no el `catch`; el `catch` queda solo para errores reales (403/409/429/400 o de red). Se actualizó
`test/features/session/conversation_screen_test.dart` (`_UnavailableApi.sendTurn` devuelve un
`TurnResult` degradado en vez de lanzar) — y, como cada turno ahora "tiene éxito" con `200`, la
conversación vuelve a `idle` entre envío y envío en vez de quedarse en `reviewing`: el test reabre el
modo texto en cada vuelta del bucle.

## PEND-12 · SPEC-04 §4 paso 2 · `FakeApi.sendTurn` repetía el `idx` del turno de usuario y del tutor

`TurnsService` (API real) da a cada turno —del usuario y del tutor— su propio `idx` secuencial
(`userTurnIdx = lastIdx + 1`, tutor `= userTurnIdx + 1`; la apertura ya ocupa `idx = 0`).
`FakeApi.sendTurn` incrementaba `_turnCounter` una sola vez por turno de usuario y reutilizaba ese
mismo valor para el `TurnRecord` del tutor, así que `GET /sessions/:id` con `USE_FAKE_API=true`
devolvía dos turnos con el mismo `idx` por cada intercambio. Se corrige incrementando el contador
también para el turno del tutor.

## PEND-13 · SPEC-07 §7 · `challengeFromUserId` (PEND-16 de PR-04.md) y `kind` de los desafíos aceptados

PEND-16 de `docs/specs/pendientes/PR-04.md` deja anotado que `apps/mobile` no mandaba
`challengeFromUserId` al aceptar un desafío (`CreateSessionDto` ya lo acepta como UUID opcional
desde PR-04 y `close_session` lo usa para `XP_CHALLENGE_BONUS`, SPEC-07 §7); sin ese campo nadie
podía cobrar el bono desde la app. `group_screen.dart::_acceptChallenge` también mandaba
`kind: 'topic'` (el mismo bug de PEND-09, corregido ahí para el resto de la app).

**Corrección:** `FluentApi.createSession` agrega `challengeFromUserId` (opcional, se manda tal cual
al DTO real); `_acceptChallenge` lo pasa con `challenge.fromUserId`.

**Decisión sin spec — `kind` del desafío aceptado:** SPEC-07 §7 dice «al aceptar, la app abre
`POST /sessions` con `kind` y `topic` iguales» (al desafío original), pero `GET /challenges`
(`ChallengeItem`) solo expone `topic` y `kind`, no `roleplayId` ni `newsItemId` — y
`sessions.service.ts::resolveRoleplay`/`resolveNews` exigen esos ids para `kind='roleplay'`/`'news'`
(SPEC-04 §3.2). Un desafío que originalmente fue un roleplay o una noticia no se puede reabrir con su
`kind` real solo con lo que trae `ChallengeItem`: mandarlo tal cual daría siempre `400 VALIDATION`.
**Se abre siempre como `free_topic`** con el `topic` legible del desafío (que ya es un texto apto
para `free_topic`, PEND-11 de `docs/specs/pendientes/PR-04.md` — es `roleplay.title_es`/
`newsItem.title`/el texto libre según el caso), en vez de propagar `challenge.kind`. Es la única
lectura de "kind y topic iguales" que funciona contra el contrato real sin tocar `apps/api`
(fuera del alcance de este PR) para exponer esos ids en `GET /challenges`. Si en el futuro se
necesita reabrir el roleplay/noticia exactos, hay que ampliar esa respuesta primero.
