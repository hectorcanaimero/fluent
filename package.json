{
  "name": "fluent",
  "private": true,
  "packageManager": "pnpm@9.15.2",
  "scripts": {
    "api": "pnpm --filter @fluent/api",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "pnpm": {
    "overrides": {
      "undici": ">=6.27.0",
      "tmp": ">=0.2.6",
      "multer": ">=2.3.0"
    }
  }
}
